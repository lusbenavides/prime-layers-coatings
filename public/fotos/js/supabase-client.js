// Cliente auxiliar de Supabase (Opcional para integraciones directas en el Frontend)
export const supabaseConfig = {
    url: window.location.origin,
    info: "Las operaciones seguras de Prime Layer Coatings corren del lado del servidor en /api"
};