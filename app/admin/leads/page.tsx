import { LeadsView } from '@/components/admin/LeadsView';

export const metadata = { title: 'Leads | Prime Layer CRM', robots: { index: false } };

export default function LeadsPage() {
  return <LeadsView />;
}
