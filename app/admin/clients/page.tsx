import { ClientsView } from '@/components/admin/ClientsView';

export const metadata = { title: 'Clients | Prime Layer CRM', robots: { index: false } };

export default function ClientsPage() {
  return <ClientsView />;
}
